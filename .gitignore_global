.idea/
/.cache/
/.eggs/
/.clang_complete
.vscode

# For ~/.fonts generating .uuid files on fc-cache(1)
.uuid

# Ignore environment variable files (with optional numeric suffix)
.env
.env[0-9]*
env
env[0-9]*

# Ignore Python virtual environment directories (with optional numeric suffix)
.venv
.venv[0-9]*
venv
venv[0-9]*
